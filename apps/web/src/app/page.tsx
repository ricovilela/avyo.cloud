// Minimal root page (Server Component). Feature pages are added by later specs;
// this proves the App Router root layout + page render on the scaffolded base.

export default function HomePage() {
  return (
    <main>
      <h1>Avyo</h1>
    </main>
  );
}
